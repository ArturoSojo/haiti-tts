version: '3.8'
services:
  ml-worker:
    build: ../ml
    volumes:
      - ../ml:/app
      - ../api:/api
    command: poetry run rq worker tts-tasks
    environment:
      - PYTHONUNBUFFERED=1
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: all
    #           capabilities: [gpu]
def synthesize(text: str):
    #    import torch
    #    from coqui_tts.utils.synthesizer import Synthesizer

    #    print(f"[WORKER] Recibido texto: {text}")
    #    print(f"[WORKER] GPU disponible: {torch.cuda.is_available()}")

    # Aquí iría la carga del modelo y síntesis real
    # Ejemplo ficticio:
    # synthesizer = Synthesizer(...)
    # audio = synthesizer.tts(text)
    # save_audio(audio, "output.wav")

    return f"Audio generado para: {text}"
